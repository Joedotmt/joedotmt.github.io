export const GAMEMODE = Object.freeze({
  normal: 0,
  mouseless: 1,
  flags: 2,
});

export const defaultStyle = Object.freeze({
  color: "#3388ff",
  weight: 2,
  opacity: 1,
  fillColor: "#3388ff",
  fillOpacity: 0.3,
});

export const highlightStyle = Object.freeze({
  color: "#ff7800",
  weight: 4,
  fillOpacity: 0.5,
});

export const guessedStyle = Object.freeze({
  color: "#28a745",
  weight: 2,
  opacity: 1,
  fillColor: "#28a745",
  fillOpacity: 0.6,
  interactive: false,
});
